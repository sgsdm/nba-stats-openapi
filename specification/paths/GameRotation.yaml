get:
  summary: GameRotation
  operationId: GameRotation
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/GameID.yaml
  - $ref: ../parameters/LeagueID.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  GameID:
                    $ref: ../schemas/GameID.yaml
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
              resource:
                const: gamerotation
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/AwayTeam.yaml
                - type:
                    $ref: ../data_sets/HomeTeam.yaml
      '404':
        description: '[to do]'
