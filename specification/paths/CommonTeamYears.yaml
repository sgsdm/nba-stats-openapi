get:
  summary: CommonTeamYears
  operationId: CommonTeamYears
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/LeagueID.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
              resource:
                const: commonteamyears
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/TeamYears.yaml
      '404':
        description: '[to do]'
