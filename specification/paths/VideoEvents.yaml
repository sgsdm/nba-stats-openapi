get:
  summary: VideoEvents
  operationId: VideoEvents
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/GameEventID.yaml
  - $ref: ../parameters/GameID.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  GameEventID:
                    $ref: ../schemas/GameEventID.yaml
                  GameID:
                    $ref: ../schemas/GameID.yaml
              resource:
                const: videoevents
              resultSets:
                type: array
                items: []
      '404':
        description: '[to do]'
