get:
  summary: HustleStatsBoxScore
  operationId: HustleStatsBoxScore
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/GameID.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  GameID:
                    $ref: ../schemas/GameID.yaml
              resource:
                const: hustlestatsboxscore
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/HustleStatsAvailable.yaml
                - type:
                    $ref: ../data_sets/PlayerStats.yaml
                - type:
                    $ref: ../data_sets/TeamStats.yaml
      '404':
        description: '[to do]'
