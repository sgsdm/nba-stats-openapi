get:
  summary: FranchiseLeaders
  operationId: FranchiseLeaders
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/LeagueID.yaml
  - $ref: ../parameters/TeamID.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
                  TeamID:
                    $ref: ../schemas/TeamID.yaml
              resource:
                const: franchiseleaders
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/FranchiseLeaders.yaml
      '404':
        description: '[to do]'
