get:
  summary: CommonPlayerInfo
  operationId: CommonPlayerInfo
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/LeagueID.yaml
  - $ref: ../parameters/PlayerID.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
                  PlayerID:
                    $ref: ../schemas/PlayerID.yaml
              resource:
                const: commonplayerinfo
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/AvailableSeasons.yaml
                - type:
                    $ref: ../data_sets/CommonPlayerInfo.yaml
                - type:
                    $ref: ../data_sets/PlayerHeadlineStats.yaml
      '404':
        description: '[to do]'
