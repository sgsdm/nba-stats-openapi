get:
  summary: CommonAllPlayers
  operationId: CommonAllPlayers
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/IsOnlyCurrentSeason.yaml
  - $ref: ../parameters/LeagueID.yaml
  - $ref: ../parameters/Season.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  IsOnlyCurrentSeason:
                    $ref: ../schemas/IsOnlyCurrentSeason.yaml
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
                  Season:
                    $ref: ../schemas/Season.yaml
              resource:
                const: commonallplayers
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/CommonAllPlayers.yaml
      '404':
        description: '[to do]'
