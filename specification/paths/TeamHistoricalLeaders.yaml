get:
  summary: TeamHistoricalLeaders
  operationId: TeamHistoricalLeaders
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/LeagueID.yaml
  - $ref: ../parameters/SeasonID.yaml
  - $ref: ../parameters/TeamID.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
                  SeasonID:
                    $ref: ../schemas/SeasonID.yaml
                  TeamID:
                    $ref: ../schemas/TeamID.yaml
              resource:
                const: teamhistoricalleaders
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/CareerLeadersByTeam.yaml
      '404':
        description: '[to do]'
