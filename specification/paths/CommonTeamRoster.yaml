get:
  summary: CommonTeamRoster
  operationId: CommonTeamRoster
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/LeagueID.yaml
  - $ref: ../parameters/Season.yaml
  - $ref: ../parameters/TeamID.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
                  Season:
                    $ref: ../schemas/Season.yaml
                  TeamID:
                    $ref: ../schemas/TeamID.yaml
              resource:
                const: commonteamroster
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/Coaches.yaml
                - type:
                    $ref: ../data_sets/CommonTeamRoster.yaml
      '404':
        description: '[to do]'
