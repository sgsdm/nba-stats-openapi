get:
  summary: ISTStangings
  operationId: ISTStangings
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/LeagueID.yaml
  - $ref: ../parameters/Season.yaml
  - $ref: ../parameters/Section.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
                  Season:
                    $ref: ../schemas/Season.yaml
                  Section:
                    $ref: ../schemas/Section.yaml
              resource:
                const: iststangings
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/Standings.yaml
      '404':
        description: '[to do]'
