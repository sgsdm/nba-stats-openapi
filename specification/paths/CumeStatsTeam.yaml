get:
  summary: CumeStatsTeam
  operationId: CumeStatsTeam
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/GameIDs.yaml
  - $ref: ../parameters/LeagueID.yaml
  - $ref: ../parameters/Season.yaml
  - $ref: ../parameters/SeasonType.yaml
  - $ref: ../parameters/TeamID.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  GameIDs:
                    $ref: ../schemas/GameIDs.yaml
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
                  Season:
                    $ref: ../schemas/Season.yaml
                  SeasonType:
                    $ref: ../schemas/SeasonType.yaml
                  TeamID:
                    $ref: ../schemas/TeamID.yaml
              resource:
                const: cumestatsteam
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/GameByGameStats.yaml
                - type:
                    $ref: ../data_sets/TotalTeamStats.yaml
      '404':
        description: '[to do]'
