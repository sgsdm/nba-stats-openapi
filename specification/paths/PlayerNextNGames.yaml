get:
  summary: PlayerNextNGames
  operationId: PlayerNextNGames
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/LeagueID.yaml
  - $ref: ../parameters/NumberOfGames.yaml
  - $ref: ../parameters/PlayerID.yaml
  - $ref: ../parameters/Season.yaml
  - $ref: ../parameters/SeasonType.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
                  NumberOfGames:
                    $ref: ../schemas/NumberOfGames.yaml
                  PlayerID:
                    $ref: ../schemas/PlayerID.yaml
                  Season:
                    $ref: ../schemas/Season.yaml
                  SeasonType:
                    $ref: ../schemas/SeasonType.yaml
              resource:
                const: playernextngames
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/NextNGames.yaml
      '404':
        description: '[to do]'
