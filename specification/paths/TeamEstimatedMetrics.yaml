get:
  summary: TeamEstimatedMetrics
  operationId: TeamEstimatedMetrics
  description: '[to do]'
  security: []
  tags:
  - nba
  parameters:
  - $ref: ../parameters/LeagueID.yaml
  - $ref: ../parameters/Season.yaml
  - $ref: ../parameters/SeasonType.yaml
  responses:
    '200':
      description: '[to do]'
      content:
        application/json:
          schema:
            type: object
            properties:
              parameters:
                type: object
                properties:
                  LeagueID:
                    $ref: ../schemas/LeagueID.yaml
                  Season:
                    $ref: ../schemas/Season.yaml
                  SeasonType:
                    $ref: ../schemas/SeasonType.yaml
              resource:
                const: teamestimatedmetrics
              resultSets:
                type: array
                items:
                - type:
                    $ref: ../data_sets/TeamEstimatedMetrics.yaml
      '404':
        description: '[to do]'
